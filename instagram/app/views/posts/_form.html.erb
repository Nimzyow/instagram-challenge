<%= form_for @post, html: { multipart: true } do |f| %>

  <%= f.file_field :image, class: "form-control" %>
  <%= f.hidden_field :image_cache %>

  <%#<%= form.label :post %>
  <%#<%= form.text_area :post, :cols => 30, :rows => 10 %>
  
  <%= f.submit "Save Post", class: "btn btn-success" %>
<% end %>